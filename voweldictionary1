def letter_idx(text): 
  vowels = 'aeiouy'
  string = text.lower()
  count_vowels = {}.fromkeys(vowels, 0)
 
  for x in string:
    if x in count_vowels:
        count_vowels[x] += 1
 
 return print(count_vowels)
